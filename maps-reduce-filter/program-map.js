//! it return a new array, it is basically used to compute an array, peform some operation on it
let nums = [1, 2, 3, 4];
// ! Let add each element by one
let addOne = nums.map((value) => value + 1);

console.log(addOne); //[ 2, 3, 4, 5 ]
